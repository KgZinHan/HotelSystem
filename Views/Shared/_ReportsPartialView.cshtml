@model ReportsModel

<div class="table-responsive mt-2">
    <div class="container">

        @if (Model.ReportTitleNo == 1)
        {
            <div class="card card-gray">
                <div class="card-header">
                    <h3 class="card-title">@Model.ReportTitle</h3>
                </div>
                <div class="card-body">
                    <form asp-action="RoomAvailableOccupancyPrint">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <div class="form-group row">
                            <label class="col-form-label col-4">From Date</label>
                            <input type="date" asp-for="@Model.FromDate" class="form-control col-6" />
                        </div>
                        <div class="form-group row">
                            <label class="col-form-label col-4">To Next</label>
                            <input type="number" asp-for="@Model.ToNext" class="form-control col-6" />
                        </div>
                        <div class="form-group">
                            <input type="submit" value="Report" class="btn btn-primary" />
                        </div>
                    </form>
                </div>
            </div>
        }
        @if (Model.ReportTitleNo == 2)
        {
            <div class="card card-gray">
                <div class="card-header">
                    <h3 class="card-title">@Model.ReportTitle</h3>
                </div>
                <div class="card-body">
                    <form asp-action="OutOfOrderRoomPrint">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <div class="form-group row">
                            <label class="col-form-label col-4">From Date</label>
                            <input type="date" asp-for="@Model.FromDate" class="form-control col-6" />
                        </div>
                        <div class="form-group row">
                            <label class="col-form-label col-4">To Next</label>
                            <input type="number" asp-for="@Model.ToNext" class="form-control col-6" />
                        </div>
                        <div class="form-group">
                            <input type="submit" value="Report" class="btn btn-primary" />
                        </div>
                    </form>
                </div>
            </div>
        }
        @if (Model.ReportTitleNo == 3)
        {
            <div class="card card-gray">
                <div class="card-header">
                    <h3 class="card-title">@Model.ReportTitle</h3>
                </div>
                <div class="card-body">
                    <form asp-action="InHouseGuestPrint">
                        <div class="form-group">
                            <input type="submit" value="Report" class="btn btn-primary" />
                        </div>
                    </form>
                </div>
            </div>
        }
        @if (Model.ReportTitleNo == 4)
        {
            <div class="card card-gray">
                <div class="card-header">
                    <h3 class="card-title">@Model.ReportTitle</h3>
                </div>
                <div class="card-body">
                    <form asp-action="ExpectedCheckInPrint">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <div class="form-group row">
                            <label class="col-form-label col-4">Check-In Date</label>
                            <input type="date" asp-for="@Model.FromDate" class="form-control col-6" />
                        </div>
                        <div class="form-group">
                            <input type="submit" value="Report" class="btn btn-primary" />
                        </div>
                    </form>
                </div>
            </div>
        }
        @if (Model.ReportTitleNo == 5)
        {
            <div class="card card-gray">
                <div class="card-header">
                    <h3 class="card-title">@Model.ReportTitle</h3>
                </div>
                <div class="card-body">
                    <form asp-action="ExpectedActualCheckInPrint">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <div class="form-group row">
                            <label class="col-form-label col-4">Check-In Date</label>
                            <input type="date" asp-for="@Model.FromDate" class="form-control col-6" />
                        </div>
                        <div class="form-group">
                            <input type="submit" value="Report" class="btn btn-primary" />
                        </div>
                    </form>
                </div>
            </div>
        }
        @if (Model.ReportTitleNo == 6)
        {
            <div class="card card-gray">
                <div class="card-header">
                    <h3 class="card-title">@Model.ReportTitle</h3>
                </div>
                <div class="card-body">
                    <form asp-action="ExpectedCheckOutPrint">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <div class="form-group row">
                            <label class="col-form-label col-4">Check-Out Date</label>
                            <input type="date" asp-for="@Model.FromDate" class="form-control col-6" />
                        </div>
                        <div class="form-group">
                            <input type="submit" value="Report" class="btn btn-primary" />
                        </div>
                    </form>
                </div>
            </div>
        }
        @if (Model.ReportTitleNo == 7)
        {
            <div class="card card-gray">
                <div class="card-header">
                    <h3 class="card-title">@Model.ReportTitle</h3>
                </div>
                <div class="card-body">
                    <form asp-action="ExpectedActualCheckOutPrint">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <div class="form-group row">
                            <label class="col-form-label col-4">Check-Out Date</label>
                            <input type="date" asp-for="@Model.FromDate" class="form-control col-6" />
                        </div>
                        <div class="form-group">
                            <input type="submit" value="Report" class="btn btn-primary" />
                        </div>
                    </form>
                </div>
            </div>
        }
        @if (Model.ReportTitleNo == 8)
        {
            <div class="card card-gray">
                <div class="card-header">
                    <h3 class="card-title">@Model.ReportTitle</h3>
                </div>
                <div class="card-body">
                    <form asp-action="NoShowCancelListingPrint">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <div class="form-group row">
                            <label class="col-form-label col-4">Hotel Date</label>
                            <input type="date" asp-for="@Model.FromDate" class="form-control col-6" />
                        </div>
                        <div class="form-group">
                            <input type="submit" value="Report" class="btn btn-primary" />
                        </div>
                    </form>
                </div>
            </div>

        }
        @if (Model.ReportTitleNo == 9)
        {
            <div class="card card-gray">
                <div class="card-header">
                    <h3 class="card-title">@Model.ReportTitle</h3>
                </div>
                <div class="card-body">
                    <form asp-action="ExtendStayListingPrint">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <div class="form-group row">
                            <label class="col-form-label col-4">Hotel Date</label>
                            <input type="date" asp-for="@Model.FromDate" class="form-control col-6" />
                        </div>
                        <div class="form-group">
                            <input type="submit" value="Report" class="btn btn-primary" />
                        </div>
                    </form>
                </div>
            </div>

        }
        @if (Model.ReportTitleNo == 10)
        {
            <div class="card card-gray">
                <div class="card-header">
                    <h3 class="card-title">@Model.ReportTitle</h3>
                </div>
                <div class="card-body">
                    <form asp-action="TodayTransferUpgradePrint">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <div class="form-group row">
                            <label class="col-form-label col-4">Hotel Date</label>
                            <input type="date" asp-for="@Model.FromDate" class="form-control col-6" />
                        </div>
                        <div class="form-group">
                            <input type="submit" value="Report" class="btn btn-primary" />
                        </div>
                    </form>
                </div>
            </div>

        }
        @if (Model.ReportTitleNo == 11)
        {
            <div class="card card-gray">
                <div class="card-header">
                    <h3 class="card-title">@Model.ReportTitle</h3>
                </div>
                <div class="card-body">
                    <form asp-action="AllActivitiesPrint">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <div class="form-group row">
                            <label class="col-form-label col-4">Hotel Date</label>
                            <input type="date" asp-for="@Model.FromDate" class="form-control col-6" />
                        </div>
                        <div class="form-group">
                            <input type="submit" value="Report" class="btn btn-primary" />
                        </div>
                    </form>
                </div>
            </div>

        }
        @if (Model.ReportTitleNo == 12)
        {
            <div class="card card-gray">
                <div class="card-header">
                    <h3 class="card-title">@Model.ReportTitle</h3>
                </div>
                <div class="card-body">

                    <form asp-action="RoomingListPrint">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <div class="form-group row">
                            <label class="col-form-label col-4">Arrive Date</label>
                            <input type="date" asp-for="@Model.FromDate" class="form-control col-6" />
                        </div>
                        <div class="form-group">
                            <input type="submit" value="Report" class="btn btn-primary" />
                        </div>
                    </form>
                </div>
            </div>

        }
    </div>
    </div>
</div>
